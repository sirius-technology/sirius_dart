abstract class Controller {
  // The constructor can be used for setting common configurations, like base path
  // final String basePath;

  // Controller(this.basePath);

  // // You can also add common logic here, for example, logging or validation.
  // // By default, routes will map to these methods:
  // void before() {}
  // void after() {}

  // // These methods can be overridden by users to define specific actions
  // void index(HttpRequest request) {}
}
